{
  "name": "@holo/tsconfig",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript configuration presets",
  "license": "MIT",
  "files": ["*.json"],
  "scripts": {
    "build": "echo 'no build for @holo/tsconfig'",
    "test": "echo 'no tests for @holo/tsconfig'",
    "typecheck": "echo 'no typecheck for @holo/tsconfig'",
    "lint": "echo 'no lint for @holo/tsconfig'"
  }
}
